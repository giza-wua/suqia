# سجل الإصدارات

| الإصدار | أهم التغييرات |
|---|---|
| 1.0.3 | أول تسليم: القرارات المعمارية (PBKDF2+JWT، Polling بدل Realtime)، سكيما D1، نمط مبدئي للـ Worker (مساقي + بلاغات فقط) |
| 1.0.4 | اكتمال كل الـ routes (الترع+سجل التطهير، الكباري، الآبار، المصارف، الأعضاء، الأخبار، المستخدمين) — Backend كامل |
| 1.0.5 | أدوات ترحيل بيانات Firestore الحقيقية (تبيّن لاحقاً إنها مش مطلوبة لعدم وجود بيانات Firestore حية) |
| 1.0.6 | تبسيط لمشروع واحد موحَّد (Worker + Assets)، حذف أدوات ترحيل Firestore غير المطلوبة، إضافة `create-first-admin.js` |
| 1.0.7 | تنظيف شامل: ملفات الموقع بقت في الجذر مباشرة، حذف `firestore.rules` والمستندات القديمة، تثبيت رقم الإصدار في الكود |
| 1.0.8 | إصلاح مطابقة الحقول عند استعادة نسخ احتياطية قديمة (camelCase للكباري/الآبار/المصارف، وحقل `status_after` لسجل تطهير الترع) |
| 1.0.9 | تسجيل الدخول بقى باسم المستخدم فقط (قرار نهائي)؛ زرار منفصل لكل قسم بيانات في الإعدادات (تنزيل/استيراد) مع شريط تقدّم ورسالة اكتمال دقيقة |
| 1.0.10 | حذف كل التعليقات من `schema.sql` (للتنفيذ اليدوي في كونسول D1)؛ إنشاء مجلد `docs/` لأول مرة (schema, architecture, api) |
| 1.0.11 | زرار "تحديد الموقع" (GPS) في الترع/الكباري/الآبار/المصارف؛ فافيكون موحَّد؛ نصوص الجهة المسؤولة في الهيدر/الفوتر؛ تحسينات انيميشن |
| 1.0.12 | ميزة "تغيير كلمة مروري" الذاتية (أي مستخدم يقدر يغيّرها بنفسه) |
| 1.0.13 | شاشة "إعداد أول مرة" حقيقية (`/admin/setup.html`) بدل الاعتماد على SQL يدوي؛ توثيق الفرق بين نشر Pages و Workers |
| 1.1.2 | مراجعة شاملة للكود والملفات (تأكيد عدم وجود ملفات ميتة)؛ توسيع `docs/` لحزمة توثيق احترافية كاملة (deployment, frontend, security, troubleshooting, changelog) |
| 1.1.3 | إضافة `_worker.js` جاهز في الجذر (مبنيّ من `src/`) عشان المشروع يشتغل على Cloudflare Pages (Advanced Mode) كمان، مش Worker بس — لمين مضطر يستخدم Pages |
| 1.1.4 | استبدال `_worker.js` بمجلد `functions/api/[[path]].js` — نمط Pages Functions القياسي (بدل Advanced Mode)، بيطابق الطريقة العادية اللي أي مشروع Pages+D1 شغّال بيها |
| 1.1.5 | إصلاح خطأ 500 حقيقي في نشر Pages: `functions/api/[[path]].js` كان بيستورد من `src/` بمعزل عنه، وده كان بينهار وقت التجميع — بقى ملف واحد مُجمَّع بالكامل من غير أي استيراد خارجي |
| 1.1.6 | **السبب الحقيقي** وراء الـ 500 المستمر بعد 1.1.5: تشفير كلمة المرور (PBKDF2 600 ألف تكرار) بياخد ~280ms CPU حقيقي — أكبر من حد وقت المعالج لـ Cloudflare (10-30ms)، فالطلب بيفشل فوراً بدون رسالة خطأ واضحة (تجاوز حد المعالج مش استثناء JS عادي، مش بيتلقّط بـ try/catch). قلّلناها لـ 10,000 تكرار (~5ms) — تفاصيل القرار في `docs/security.md` |
| 1.1.7 | إصلاح صفحة الإعدادات اللي كانت مكسورة بالكامل (JS مش شغّال): كانت بتستورد `renderActivityLog`/`initActivityLogListener` من ملف بقى no-op من غير ما تتضاف الدوال دي، و`changeMyPassword` كان فُقِد من `auth.js` في تحديث سابق — استيراد دالة مش موجودة بيوقف كل الملف. اتصلحوا الاتنين + فحص شامل تلقائي لكل استيراد في المشروع (13 صفحة + كل ملفات js/) للتأكد إن مفيش استيراد تاني مكسور |
| 1.1.8 | كروت ملخّص سريعة (كانت موجودة في المساقي بس) بقت في الترع، البلاغات، الأخبار، الكباري، الآبار، المصارف، والأعضاء كمان — كل صفحة بقى فيها كروت إحصائية قابلة للضغط للفلترة السريعة قبل الجدول. صفحة الإعدادات (فحص حالة النظام) بقت تجيب عدد كل قسم بنفسها مباشرة بدل الاعتماد على أرقام محفوظة من صفحات تانية |
| 1.1.9 | إصلاح جوهري لموثوقية الاستيراد: كانت ثلاث شاشات مختلفة (المساقي، الأعضاء، النسخ الاحتياطي) كل واحدة عندها نسخة منفصلة من نفس منطق الاستيراد، وكلها بتبعت 10 طلبات متوازية في نفس اللحظة — ده كان بيفشل بشكل غير ثابت مع المجموعات الكبيرة (الكباري 475 سجل تحديداً). دلوقتي دالة واحدة مشتركة (`bulkImport` في `js/services/api.js`) بتبعت طلب واحد بعد التاني بالتتابع، تستخدمها كل شاشات الاستيراد في التطبيق من غير استثناء |
| 1.1.10 | إصلاح سبب حقيقي لـ"تعذر التحميل" في الترع أحياناً: تحليل بيانات المقاطع (`sections_json`) كان من غير حماية، فسجل واحد بمشكلة كان بيفشّل قائمة الترع كلها بدل السجل ده بس. + "منطقة خطر" جديدة في الإعدادات لمسح أي قسم بيانات بالكامل (بتأكيد مزدوج: كتابة اسم القسم + تأكيد نهائي)، عشان تقدر تبدأ من الصفر لو فيه بيانات قديمة أو مكررة من محاولات استيراد سابقة |
| 1.1.11 | رسائل فشل الاستيراد بقت بتوضّح السبب الحقيقي لكل سجل فشل بالتفصيل (اسم السجل + رسالة الخطأ)، مش رقم عام بس زي "5 فشل" — لو فيه مشكلة حقيقية مستقبلاً، الرسالة نفسها كافية للتشخيص من غير تخمين |
| 1.1.12 | فحص حالة النظام (صفحة الإعدادات) بقى بيوريك رسالة الخطأ الحقيقية جوّه كل كارت لو مجموعة فشلت تتحمّل، مش نص عام ثابت — والسيرفر نفسه بقى بيرجّع رسالة الخطأ الحقيقية في أي رد 500 بدل رسالة عامة، عشان أي عطل باقي يتحدد سببه بالظبط من الصورة/الرسالة نفسها |
| 1.1.13 | نظرية جديدة لعطل الترع المستمر: ملفات الاستيراد المختلفة اللي اتولّدت عبر وقت طويل من التطوير كانت كل مرة بتاخد معرّفات (IDs) عشوائية جديدة، فاستيراد نسخ مختلفة من نفس البيانات كان بيضيف صفوف جديدة بدل ما يحدّث القديمة — العدد الحقيقي في القاعدة ممكن يكون أضعاف الـ283 المتوقَّعين. أضيف حد أمان (`LIMIT 5000`) على قائمة الترع عشان القائمة تفضل شغّالة حتى لو العدد كبير، و`/api/canals/count` كأداة تشخيص سريعة (تفتحها في المتصفح وتشوف العدد الحقيقي فوراً) |
| 1.1.14 | **السبب الحقيقي المؤكَّد** (تم تشغيل الاستعلام فعلياً على الـ schema، مش تخمين نظري زي المحاولات السابقة): `GET /api/canals` كان بيرتّب النتائج بـ `ORDER BY created_at`، لكن جدول `canals` مالوش عمود `created_at` من الأساس (شوف `schema.sql`) — يعني الاستعلام كان بيفشل بخطأ SQL "no such column: created_at" في **كل** استدعاء من غير استثناء واحد، والسيرفر بيرجّع 500. الاستعادة من نسخة احتياطية كانت شغّالة صح فعلياً (كل سجل بيتخزّن بـ `POST` منفصل)، لكن أي محاولة عرض للترع (وبالأخص الفروع/"بنات الترع" اللي هي أغلب الـ283 سجل) بعد كده كانت بتنهار فوراً — فبيبان وكأن الاستعادة "مسحت" بنات الترع رغم إنها كانت موجودة فعلاً في القاعدة. تم فحص كل استعلامات SELECT/INSERT في المشروع آلياً ضد الـ schema الحقيقي للتأكد إنه مفيش عمود غلط تاني في أي مكان — النتيجة: هذا كان الاستعلام الوحيد المكسور. الإصلاح: `created_at` → `updated_at` (العمود الصحيح الموجود فعلاً)، في `src/routes/canals.js` وفي النسخة المطابقة يدوياً `functions/api/[[path]].js` (نسخة Cloudflare Pages البديلة، لسه موجودة لمين محتاجها — لاحظ إنها نسخة مكرَّرة كاملة من نفس المنطق، وهي بالظبط سبب انحراف زي ده؛ يستاهل التفكير مستقبلاً في توحيدها بدل نسختين منفصلتين) |
| 1.1.15 | تأكيد من صاحب المشروع إن النشر فعلياً Cloudflare Pages + D1 (مش Workers) — يعني `functions/api/[[path]].js` هو الكود الحي فعلاً. ثلاث نتائج: **(1)** إصلاح ظاهرة "الحالة null" الحرفية في جدول الترع (وأماكن مشابهة في المساقي والبلاغات): كانت قيمة `status` الفاضية بترجع `NULL` من القاعدة وتتحط جوّه template literal من غير حماية، فـ JavaScript بيحوّلها لنص "null" ظاهر في الواجهة بدل ما تتحول لنص بديل واضح. **(2)** تأكيد إن عطل "الخريطة الموحَّدة فاضية" كان نفس سبب عطل 1.1.14 بالظبط: الخريطة بتحمّل كل المجموعات (ترع/كباري/آبار/مصارف) مع بعض بـ `Promise.all`، وفشل تحميل الترع وحده كان كافي يوقف الخريطة كلها — اتحل تلقائياً بإصلاح 1.1.14. **(3)** رجّعنا "كرت عرض التفاصيل الكامل" اللي كان في سُقيا قبل الدمج: بالدوس على اسم أي سجل (ترعة/كبري/بئر/مصرف/مسقى) أو زرار "👁️ عرض" بيفتح كرت منظَّم بكل بياناته من نموذج الإدخال؛ للترع تحديداً فيه كمان رسم للقطاع العرضي وجدول المقاطع الهندسية وبيانات التبطين، زي التصميم القديم بالظبط |
| 1.1.16 | تنظيف معماري: اتشال مجلد `src/` (نسخة Workers-style موازية من منطق الـ API) بالكامل، بناءً على تأكيد صريح إن النشر Pages+D1 فقط. `functions/api/[[path]].js` بقى مصدر الحقيقة الوحيد لمنطق الـ API — مفيش نسختين محتاجين مزامنة يدوية بعد كده (وده كان سبب باجي 1.1.5 و1.1.14). اتحدَّث معاه `wrangler.toml` (اتشال `main`/`[assets]` الخاصين بـ Workers، مالهمش أي تأثير حقيقي على نشر Pages)، و`.assetsignore`، و`README.md` (كان لسه بيقول 1.1.2)، و`docs/architecture.md`، و`docs/deployment.md` (بقى موثِّق لطريقة واحدة بس بدل طريقتين)، و`docs/security.md`، وتعليقات متفرقة كانت بتشاور على ملفات `src/` اللي بقت مش موجودة |
| 1.1.17 | **السبب الحقيقي المؤكَّد بفحص AST فعلي** (acorn + `node --input-type=module --check`، مش تخمين) وراء عطل كامل في صفحة الترع: اختفاء تام للسايد بار والتوب بار وعنوان الصفحة، وجدول الترع وكروت الملخّص فاضية دايماً بـ"-". السبب: سطر تعريف الدالة `export function updateCanalsStatusSummary() {` في `js/data/canals.js` كان **ضايع بالكامل** (على الأغلب حذف عن طريق الخطأ في تعديل سابق)، وجسم الدالة فضل معلَّق كـ كود على مستوى الملف نفسه، وراه قوس إغلاق `}` يتيم من غير قوس فتح يقابله — ده **SyntaxError حقيقي**، اتأكَّد بمحلِّلين مستقلين (acorn، و`node --input-type=module --check`؛ فحص `node --check` العادي من غير تحديد وضع الموديول كان بيدّي نتيجة مضلِّلة "سليم" رغم إن الملف فعلاً مكسور). موديول فيه SyntaxError بيفشل يتحمَّل بالكامل في المتصفح — يعني أي صفحة بتستورد `canals.js` (الترع، وكمان الخريطة الموحَّدة اللي بتحمّل كل المنشآت مع بعض) كانت بتفشل بالكامل قبل ما ينفَّذ أي سطر فيها، حتى `renderAdminShell()` نفسها اللي برسم السايد بار — مش مشكلة بيانات أو API خالص، ومفهاش أي علاقة بإصلاحات 1.1.14/1.1.15 السابقة. الإصلاح: رجّعنا هيدر الدالة في مكانه. اتعمل كمان فحص AST شامل (مش استيراد/تصدير سطحي بس زي 1.1.7) على **كل** ملفات `js/*.js` وكل `<script type="module">` جوّه صفحات `admin/*.html` — النتيجة كلها نضيفة، مفيش نفس النوع ده من العطل في أي ملف تاني حالياً |
| 1.1.18 | ثلاث حاجات بطلب صريح: **(1)** فلتر "الهندسة" في الترع كان بيبان شغال بس من غير أي تأثير — منطق `renderCanals()` صح 100% برمجياً وفحصنا شجرة الاستيراد/التصدير وكل الـ DOM ids فمفيش باج برمجي واضح، فالسبب الأرجح بيانات ترع قديمة (من `tools/migrate-data.js` أو نسخة احتياطية مُستعادة) بقيمة "هندسة" مش مطابقة 100% لأي واحدة من الـ 6 الثابتة (فرق مسافات أو صياغة من نظام سُقيا القديم) — الإصلاح: مقارنة متسامحة (`normalizeText` في `utils/ui-helpers.js`: تطبيع Unicode + حذف مسافات زايدة) + مزامنة تلقائية لقائمة الفلتر مع أي قيمة هندسة حقيقية موجودة بالبيانات مش من ضمن الـ 6 (`syncDirectorateFilterOptions`)، عشان أي سجل حقيقي يبقى قابل للفلترة أياً كانت صيغته بالظبط؛ نفس التحسين اتطبَّق على فلتر الهندسة في المصارف. **(2)** مسح كامل لميزة "سجل أعمال الترع" من `admin/canals.html` (اختيار ترعة وعرض/إضافة أحداث تاريخية بحالتها) وكل دوالها من `js/data/canals.js` (`populateCanalHistorySelect`, `filterCanalHistorySelect`, `renderCanalHistory`, `addCanalHistoryEntry`) — حالة الترعة بقت تتحدَّث من فورم الإضافة/التعديل مباشرة بس؛ نقطة نهاية `/api/canals/:id/history` اتسابت عمداً لأنها لسه مُستخدمة في `data/backup.js` لحفظ/استعادة أي بيانات تاريخية قديمة اتسجلت فعلاً. **(3)** توحيد واجهة الإدخال: تصدير Excel + لوحة استيراد Excel (بنفس شكل المساقي بالظبط: قالب فاضي للتحميل + منطقة سحب وإفلات) اتضافت لصفحات الترع والكباري والمصارف والآبار، كل واحدة بدالة `handleExcelImport` وقالب Excel مخصوص بأعمدتها في `utils/excel-export.js`. فحص AST شامل + فحص شجرة استيراد/تصدير كامل بعد كل التعديلات — كله نضيف |
| 1.2.0 | **مفيش أي تغيير وظيفي جديد هنا** — نفس محتوى 1.1.18 بالظبط (فحصنا الملف المُسلَّم فعلياً بايت لبايت وأكَّدنا إن كل تغييرات 1.1.18 موجودة فيه صح). قفزنا رقم الإصدار قفزة كاملة لـ 1.2.0 (مش 1.1.19) بناءً على طلب صريح من الأمين، بعد ما أبلغ إن الموقع المنشور على `zimam.pages.dev` لسه بيبيّن 1.1.17 وميزة "سجل أعمال الترع" لسه ظاهرة حتى بعد رفع نسخة 1.1.18 وتجربة أكتر من متصفح — يعني المشكلة مش كاش متصفح عادي (كان يتحل بـ Hard Refresh). القفزة لرقم مختلف بوضوح (1.2.0 بدل أي 1.1.x) هدفها إزالة أي التباس بصري نهائياً: أي حد يشوف الرقم في أعلى الصفحة أو في `admin/login.html` يتأكد فوراً إنه شايف النسخة الجديدة فعلاً ولا لأ، من غير ما يعتمد على تذكّر آخر رقمين كانوا إيه. **مهم:** لو النسخة دي كمان اتفتحت وظهر رقم غير 1.2.0 بعد رفعها والتأكد من نجاح الـ deployment على Cloudflare Pages، فده معناه المشكلة مش في الكود ولا في الملف المُسلَّم خالص، لكن في مكان تاني في مسار النشر (مسار رفع خطأ، فرع Git مختلف، إعداد Cloudflare Pages بيقرأ من مصدر تاني، أو Preview deployment بدل Production) — يستاهل مراجعة إعدادات النشر نفسها مباشرة |
| 1.2.1 | **السبب الحقيقي المؤكَّد بلقطة شاشة فعلية** (تطبيق سُقيا القديم + بيانات D1 الحقيقية، مش تخمين) وراء "فلتر الهندسة في الترع بيدّي فاضي والاسم بس اللي شغال، والقائمة فيها تكرار": عمود "الهندسة" في بيانات الترع الفعلية مخزَّن بالاسم المجرَّد بس (زي "الرياح"، "العياط") — نفس اتفاقية سُقيا الأصلي اللي اتهاجرت منه بيانات الترع كلها — بينما قائمة الفلتر وفورم الإضافة كانوا بصيغة "هندسة ري X" (بالبادئة) اللي ماكانتش بتطابق أي سجل تقريباً، وإصلاح 1.1.18 (بيضيف أي قيمة هندسة حقيقية كخيار إضافي) كان بيضيف الأسماء القصيرة الصح كخيارات "زيادة" جنب الطويلة الأصلية — نفس الهندسة بتبان مرتين (تكرار)، واحدة فاضية دايماً والتانية هي الشغالة فعلاً. الإصلاح الجذري: قائمة الفلتر وفورم الترع بقوا بالاسم المجرَّد (مطابق لسُقيا وللبيانات الفعلية)، والمقارنة بقت كمان بتجرّد بادئة "هندسة ري" لو لقتها في أي سجل قديم عشان يفضل قابل للفلترة. **مهم:** الإصلاح اقتصر على الترع تحديداً لأنها الوحيدة اللي بياناتها جات من هجرة سُقيا؛ باقي الشاشات (المساقي، المصارف، الأعضاء، البلاغات) بياناتها اتدخلت من فورم زمام نفسه من الأول وبالتالي متسقة فعلاً بصيغة "هندسة ري X" — تغييرها كان هيعمل نفس المشكلة بالظبط في الاتجاه المعاكس، فاتسابت زي ما هي عمداً. إضافات تانية بنفس الطلب: فلتر "المناوبة" (أ/ب/جـ) للترع (البيانات موجودة من قبل في كل مقطع هندسي، بس ماكانتش معروضة كفلتر)، وعدّاد نتائج حي بجانب أزرار الفلتر (زي "٢٧٦ ترعة" في سُقيا) بيتحدّث تلقائياً مع كل فلترة |
| 1.2.2 | تصحيح مسار 1.2.1 بطلب صريح: قائمة فلتر وفورم الهندسة في الترع رجعت لصيغة "هندسة ري X" الكاملة (بدل الاسم المجرَّد بس) عشان تبقى متسقة مع باقي الشاشات (المصارف، المساقي). مقارنة الفلتر لسه بتجرّد البادئة من الطرفين قبل المقارنة (`normDirectorate`)، فالفلتر لسه بيطابق صح سواء السجل مخزَّن مجرَّد (سُقيا القديمة) أو كامل (سجلات جديدة) — مضفناش رجوع للمشكلة الأصلية. وضفنا `formatDirectorate` (عكس `normDirectorate`) بيتعرض بيه اسم الهندسة في كل مكان: الجدول، فورم التعديل (كان بيفضل فاضي لأي ترعة قديمة لأنه بيحط القيمة المجرَّدة في select قايمته كاملة!)، كرت التفاصيل، **وكمان لوحة "حالة الترع" العامة في الصفحة الرئيسية** — اكتشفنا إنها فيها نفس باج المقارنة الصارمة بالظبط، متلاحظش قبل كده لحد دلوقتي. الحاجة التانية: أيقونات "استيراد Excel" في كل الشاشات الست (الترع، المساقي، المصارف، الكباري، الآبار، الأعضاء) كانت بتستخدم **نفس أيقونة استعادة النسخة الاحتياطية** في الإعدادات (سهم دائري) لفعل مختلف تماماً — لبس بصري حقيقي، بالظبط زي ما اتوصف. اتفصلت لأيقونة `upload` جديدة (سهم لأعلى فوق قاعدة) مخصوصة. وأيقونتَي Excel (كانت X عام قريب من أيقونة الإغلاق) وPDF (مستطيل بخطوط عامة) اتصمّمتا من جديد: Excel بقت شبكة/جدول واضحة، PDF بقت ورقة بزاوية مطوية مع سهم تصدير. فحصنا برمجياً (مش نظرة سريعة) إن كل استخدام `icon()` في التطبيق كله بيرجّع اسم موجود فعلاً (مفيش زرار فاضي)، وإن كل الأيقونات التسعة والثلاثين بتتحلَّل كـ SVG سليم عبر DOM parser حقيقي (jsdom) مش مجرد نص |

## الأرقام المتخطّاة (1.1.0، 1.1.1)

بناءً على طلب صريح، القفزة من 1.0.13 لـ 1.1.2 مقصودة — مفيش إصدارات 1.1.0/1.1.1 فعلية.
